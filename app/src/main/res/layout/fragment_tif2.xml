<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".ui.fragments.TifIslem1Fragment">

    <data>

        <variable
            name="viewModel"
            type="com.uniyaz.mobiltif.viewmodel.TifViewModel" />

        <variable
            name="fragment"
            type="com.uniyaz.mobiltif.ui.fragments.TifFragment" />
    </data>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fillViewport="true">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:paddingStart="10dp">

            <ListView
                android:id="@+id/lvTifList"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:layout_marginEnd="8dp"
                android:layout_marginBottom="8dp"
                app:layout_constraintBottom_toTopOf="@+id/btnIslemiGerceklestir"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintHeight_max="150dp"
                app:layout_constraintHeight_min="30dp"
                app:layout_constraintHorizontal_bias="1.0"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintVertical_bias="0.0" />

            <Spinner
                android:id="@+id/spinner"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:background="@drawable/spinner_bg"
                android:entries="@array/arrayIslemTurleri"
                android:onItemSelected="@{(parent,view,pos,id)->fragment.onItemSelectedSpinner(parent,view,pos,id)}"
                android:spinnerMode="dropdown"
                android:textSize="16sp"
                app:layout_constraintEnd_toEndOf="@+id/lvTifList"
                app:layout_constraintStart_toStartOf="@+id/lvTifList"
                app:layout_constraintTop_toBottomOf="@+id/lvTifList" />

            <FrameLayout
                android:id="@+id/f_layout"
                android:layout_width="0dp"
                android:layout_height="0dp"
                app:layout_constraintBottom_toTopOf="@+id/btnIslemiGerceklestir"
                app:layout_constraintEnd_toEndOf="@+id/spinner"
                app:layout_constraintStart_toStartOf="@+id/spinner"
                app:layout_constraintTop_toBottomOf="@+id/spinner">

            </FrameLayout>

            <Button
                android:id="@+id/btnIslemiGerceklestir"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="500dp"
                android:text="İşlemi Gerçekleştir"
                app:layout_constraintStart_toStartOf="@+id/f_layout"
                app:layout_constraintEnd_toEndOf="@+id/f_layout"
                app:layout_constraintBottom_toBottomOf="parent"
                />

        </androidx.constraintlayout.widget.ConstraintLayout>
    </ScrollView>
</layout>
